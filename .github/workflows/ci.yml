name: CI

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lister les dossiers à la racine
        run: ls -la
      - name: Essayer d'entrer dans le jeu 1
        run: ls -la "Two Ships Passing"